<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Localisation GPS optionnelle du commerce à l'inscription (gratuit,
         pas de clé API Google Maps) -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <application
        android:label="echango_promo"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
            <!-- App Links (partage d'une promo, croissance organique) —
                 android:autoVerify déclenche la vérification du fichier
                 https://promo.echango.com/.well-known/assetlinks.json au
                 démarrage : tant qu'il est vide (app pas encore publiée,
                 voir docs/DEPLOIEMENT_STORES.md), la vérification échoue
                 silencieusement et le lien s'ouvre dans le navigateur —
                 aucun risque à activer ce bloc dès maintenant. `/p` (pas
                 `/promo`) : chemin dédié au lien public (voir
                 AppLinksController côté backend), distinct de la route
                 interne /promo/:id pour ne jamais entrer en collision avec
                 l'API JSON GET /promo/:id si les deux finissent sur le même
                 sous-domaine. go_router route déjà /p/:id vers le même
                 écran que /promo/:id (router.dart), aucun code Dart
                 supplémentaire nécessaire une fois la vérification passée. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW"/>
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>
                <data
                    android:scheme="https"
                    android:host="promo.echango.com"
                    android:pathPrefix="/p"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
